spring:
  application:
    name: task-api
  cloud:
    config:
      uri: http://localhost:8888
  jpa:
    show-sql: true
server:
  port: 8767

springdoc:
  api-docs:
    path: /api/task/v3/api-docs
  swagger-ui:
    path: /api/task/swagger-ui.html