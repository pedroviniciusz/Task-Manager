spring:
  application:
    name: task-api
  cloud:
    config:
      uri: http://localhost:8888
  jpa:
    show-sql: true
server:
  port: 8767
  servlet:
    context-path: /

logging:
  level:
    root: trace