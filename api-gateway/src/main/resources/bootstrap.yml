spring:
  application:
    name: api-gateway
  cloud:
    config:
      uri: http://localhost:8888
    gateway:
      routes:
        - id: api-auth
          uri: lb://API-AUTH
          predicates:
            - Path=/api/auth/**
server:
  port: 8765

logging:
  level:
    root: info
