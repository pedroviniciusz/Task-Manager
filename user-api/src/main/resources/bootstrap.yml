spring:
  application:
    name: user-api
  cloud:
    config:
      uri: http://localhost:8888
  jpa:
    show-sql: true
server:
  port: 8768

springdoc:
  api-docs:
    path: /api/user/v3/api-docs
  swagger-ui:
    path: /api/user/swagger-ui.html